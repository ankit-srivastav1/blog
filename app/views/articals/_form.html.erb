
<%= form_with model: @artical, local: true do |form| %>

  <% if @artical.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@artical.errors.count, "error") %> prohibited
        this artical from being saved:
      </h2>
      <ul>
        <% @artical.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
